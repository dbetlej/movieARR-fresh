<!DOCTYPE html>
<html>
    <head>
        <title>MovieARR - organize you movies.</title>
    </head>
    <body>
        <div>
            <div>MovieARR</div>
                <div>
                    <a class="btn btn-link" href="{{ route('home') }}">
                        {{ __('Login') }}
                    </a>
                    <a class="btn btn-link" href="{{ route('home') }}">
                        <br>
                        {{ __('Register') }}
                    </a>
                </div>
        </div>
    </body>
</html>
